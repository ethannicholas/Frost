-- This file was autogenerated by WriteOperators.panda, do not modify
class Operators {
    @class
    method int8() {
        def x:Int8 := 9
        def y:Int8 := 12
        Console.printLine("int8 Arithmetic")
        Console.printLine(x + y)
        Console.printLine(x - y)
        Console.printLine(x * y)
        Console.printLine(x // y)
        Console.printLine(x << 1)
        Console.printLine(x >> 1)
        Console.printLine()
        Console.printLine("int8 Bitwise")
        Console.printLine(!!x)
        Console.printLine(x && y)
        Console.printLine(x || y)
        Console.printLine(x ~~ y)
        Console.printLine()
        Console.printLine("int8 Comparisons")
        Console.printLine(x = y)
        Console.printLine(x = x)
        Console.printLine(x != y)
        Console.printLine(x != x)
        Console.printLine(x > x)
        Console.printLine(x > y)
        Console.printLine(y > x)
        Console.printLine(x >= y)
        Console.printLine(y >= x)
        Console.printLine(x >= x)
        Console.printLine(x < y)
        Console.printLine(y < x)
        Console.printLine(y < y)
        Console.printLine(x <= y)
        Console.printLine(y <= x)
        Console.printLine(y <= y)
        Console.printLine()
        Console.printLine("int8 Compound Assignment")
        x += y
        Console.printLine(x)
        x -= y
        Console.printLine(x)
        x *= y
        Console.printLine(x)
        x //= y
        Console.printLine(x)
        x <<= 1
        Console.printLine(x)
        x >>= 1
        Console.printLine(x)
    }
    @class
    method int16() {
        def x:Int16 := 126
        def y:Int16 := 519
        Console.printLine("int16 Arithmetic")
        Console.printLine(x + y)
        Console.printLine(x - y)
        Console.printLine(x * y)
        Console.printLine(x // y)
        Console.printLine(x << 1)
        Console.printLine(x >> 1)
        Console.printLine()
        Console.printLine("int16 Bitwise")
        Console.printLine(!!x)
        Console.printLine(x && y)
        Console.printLine(x || y)
        Console.printLine(x ~~ y)
        Console.printLine()
        Console.printLine("int16 Comparisons")
        Console.printLine(x = y)
        Console.printLine(x = x)
        Console.printLine(x != y)
        Console.printLine(x != x)
        Console.printLine(x > x)
        Console.printLine(x > y)
        Console.printLine(y > x)
        Console.printLine(x >= y)
        Console.printLine(y >= x)
        Console.printLine(x >= x)
        Console.printLine(x < y)
        Console.printLine(y < x)
        Console.printLine(y < y)
        Console.printLine(x <= y)
        Console.printLine(y <= x)
        Console.printLine(y <= y)
        Console.printLine()
        Console.printLine("int16 Compound Assignment")
        x += y
        Console.printLine(x)
        x -= y
        Console.printLine(x)
        x *= y
        Console.printLine(x)
        x //= y
        Console.printLine(x)
        x <<= 1
        Console.printLine(x)
        x >>= 1
        Console.printLine(x)
    }
    @class
    method int32() {
        def x:Int32 := 88124
        def y:Int32 := 65318
        Console.printLine("int32 Arithmetic")
        Console.printLine(x + y)
        Console.printLine(x - y)
        Console.printLine(x * y)
        Console.printLine(x // y)
        Console.printLine(x << 1)
        Console.printLine(x >> 1)
        Console.printLine()
        Console.printLine("int32 Bitwise")
        Console.printLine(!!x)
        Console.printLine(x && y)
        Console.printLine(x || y)
        Console.printLine(x ~~ y)
        Console.printLine()
        Console.printLine("int32 Comparisons")
        Console.printLine(x = y)
        Console.printLine(x = x)
        Console.printLine(x != y)
        Console.printLine(x != x)
        Console.printLine(x > x)
        Console.printLine(x > y)
        Console.printLine(y > x)
        Console.printLine(x >= y)
        Console.printLine(y >= x)
        Console.printLine(x >= x)
        Console.printLine(x < y)
        Console.printLine(y < x)
        Console.printLine(y < y)
        Console.printLine(x <= y)
        Console.printLine(y <= x)
        Console.printLine(y <= y)
        Console.printLine()
        Console.printLine("int32 Compound Assignment")
        x += y
        Console.printLine(x)
        x -= y
        Console.printLine(x)
        x *= y
        Console.printLine(x)
        x //= y
        Console.printLine(x)
        x <<= 1
        Console.printLine(x)
        x >>= 1
        Console.printLine(x)
    }
    @class
    method int64() {
        def x:Int64 := 4000311441
        def y:Int64 := 1991297512
        Console.printLine("int64 Arithmetic")
        Console.printLine(x + y)
        Console.printLine(x - y)
        Console.printLine(x * y)
        Console.printLine(x // y)
        Console.printLine(x << 1)
        Console.printLine(x >> 1)
        Console.printLine()
        Console.printLine("int64 Bitwise")
        Console.printLine(!!x)
        Console.printLine(x && y)
        Console.printLine(x || y)
        Console.printLine(x ~~ y)
        Console.printLine()
        Console.printLine("int64 Comparisons")
        Console.printLine(x = y)
        Console.printLine(x = x)
        Console.printLine(x != y)
        Console.printLine(x != x)
        Console.printLine(x > x)
        Console.printLine(x > y)
        Console.printLine(y > x)
        Console.printLine(x >= y)
        Console.printLine(y >= x)
        Console.printLine(x >= x)
        Console.printLine(x < y)
        Console.printLine(y < x)
        Console.printLine(y < y)
        Console.printLine(x <= y)
        Console.printLine(y <= x)
        Console.printLine(y <= y)
        Console.printLine()
        Console.printLine("int64 Compound Assignment")
        x += y
        Console.printLine(x)
        x -= y
        Console.printLine(x)
        x *= y
        Console.printLine(x)
        x //= y
        Console.printLine(x)
        x <<= 1
        Console.printLine(x)
        x >>= 1
        Console.printLine(x)
    }
    @class
    method uint8() {
        def x:UInt8 := 16
        def y:UInt8 := 15
        Console.printLine("uint8 Arithmetic")
        Console.printLine(x + y)
        Console.printLine(x - y)
        Console.printLine(x * y)
        Console.printLine(x // y)
        Console.printLine(x << 1)
        Console.printLine(x >> 1)
        Console.printLine()
        Console.printLine("uint8 Bitwise")
        Console.printLine(!!x)
        Console.printLine(x && y)
        Console.printLine(x || y)
        Console.printLine(x ~~ y)
        Console.printLine()
        Console.printLine("uint8 Comparisons")
        Console.printLine(x = y)
        Console.printLine(x = x)
        Console.printLine(x != y)
        Console.printLine(x != x)
        Console.printLine(x > x)
        Console.printLine(x > y)
        Console.printLine(y > x)
        Console.printLine(x >= y)
        Console.printLine(y >= x)
        Console.printLine(x >= x)
        Console.printLine(x < y)
        Console.printLine(y < x)
        Console.printLine(y < y)
        Console.printLine(x <= y)
        Console.printLine(y <= x)
        Console.printLine(y <= y)
        Console.printLine()
        Console.printLine("uint8 Compound Assignment")
        x += y
        Console.printLine(x)
        x -= y
        Console.printLine(x)
        x *= y
        Console.printLine(x)
        x //= y
        Console.printLine(x)
        x <<= 1
        Console.printLine(x)
        x >>= 1
        Console.printLine(x)
    }
    @class
    method uint16() {
        def x:UInt16 := 598
        def y:UInt16 := 87
        Console.printLine("uint16 Arithmetic")
        Console.printLine(x + y)
        Console.printLine(x - y)
        Console.printLine(x * y)
        Console.printLine(x // y)
        Console.printLine(x << 1)
        Console.printLine(x >> 1)
        Console.printLine()
        Console.printLine("uint16 Bitwise")
        Console.printLine(!!x)
        Console.printLine(x && y)
        Console.printLine(x || y)
        Console.printLine(x ~~ y)
        Console.printLine()
        Console.printLine("uint16 Comparisons")
        Console.printLine(x = y)
        Console.printLine(x = x)
        Console.printLine(x != y)
        Console.printLine(x != x)
        Console.printLine(x > x)
        Console.printLine(x > y)
        Console.printLine(y > x)
        Console.printLine(x >= y)
        Console.printLine(y >= x)
        Console.printLine(x >= x)
        Console.printLine(x < y)
        Console.printLine(y < x)
        Console.printLine(y < y)
        Console.printLine(x <= y)
        Console.printLine(y <= x)
        Console.printLine(y <= y)
        Console.printLine()
        Console.printLine("uint16 Compound Assignment")
        x += y
        Console.printLine(x)
        x -= y
        Console.printLine(x)
        x *= y
        Console.printLine(x)
        x //= y
        Console.printLine(x)
        x <<= 1
        Console.printLine(x)
        x >>= 1
        Console.printLine(x)
    }
    @class
    method uint32() {
        def x:UInt32 := 124870
        def y:UInt32 := 6713
        Console.printLine("uint32 Arithmetic")
        Console.printLine(x + y)
        Console.printLine(x - y)
        Console.printLine(x * y)
        Console.printLine(x // y)
        Console.printLine(x << 1)
        Console.printLine(x >> 1)
        Console.printLine()
        Console.printLine("uint32 Bitwise")
        Console.printLine(!!x)
        Console.printLine(x && y)
        Console.printLine(x || y)
        Console.printLine(x ~~ y)
        Console.printLine()
        Console.printLine("uint32 Comparisons")
        Console.printLine(x = y)
        Console.printLine(x = x)
        Console.printLine(x != y)
        Console.printLine(x != x)
        Console.printLine(x > x)
        Console.printLine(x > y)
        Console.printLine(y > x)
        Console.printLine(x >= y)
        Console.printLine(y >= x)
        Console.printLine(x >= x)
        Console.printLine(x < y)
        Console.printLine(y < x)
        Console.printLine(y < y)
        Console.printLine(x <= y)
        Console.printLine(y <= x)
        Console.printLine(y <= y)
        Console.printLine()
        Console.printLine("uint32 Compound Assignment")
        x += y
        Console.printLine(x)
        x -= y
        Console.printLine(x)
        x *= y
        Console.printLine(x)
        x //= y
        Console.printLine(x)
        x <<= 1
        Console.printLine(x)
        x >>= 1
        Console.printLine(x)
    }
    @class
    method uint64() {
        def x:UInt64 := 420032777
        def y:UInt64 := 81272990
        Console.printLine("uint64 Arithmetic")
        Console.printLine(x + y)
        Console.printLine(x - y)
        Console.printLine(x * y)
        Console.printLine(x // y)
        Console.printLine(x << 1)
        Console.printLine(x >> 1)
        Console.printLine()
        Console.printLine("uint64 Bitwise")
        Console.printLine(!!x)
        Console.printLine(x && y)
        Console.printLine(x || y)
        Console.printLine(x ~~ y)
        Console.printLine()
        Console.printLine("uint64 Comparisons")
        Console.printLine(x = y)
        Console.printLine(x = x)
        Console.printLine(x != y)
        Console.printLine(x != x)
        Console.printLine(x > x)
        Console.printLine(x > y)
        Console.printLine(y > x)
        Console.printLine(x >= y)
        Console.printLine(y >= x)
        Console.printLine(x >= x)
        Console.printLine(x < y)
        Console.printLine(y < x)
        Console.printLine(y < y)
        Console.printLine(x <= y)
        Console.printLine(y <= x)
        Console.printLine(y <= y)
        Console.printLine()
        Console.printLine("uint64 Compound Assignment")
        x += y
        Console.printLine(x)
        x -= y
        Console.printLine(x)
        x *= y
        Console.printLine(x)
        x //= y
        Console.printLine(x)
        x <<= 1
        Console.printLine(x)
        x >>= 1
        Console.printLine(x)
    }
    @class
    method main() {
        int8()
        Console.printLine()
        int16()
        Console.printLine()
        int32()
        Console.printLine()
        int64()
        Console.printLine()
        uint8()
        Console.printLine()
        uint16()
        Console.printLine()
        uint32()
        Console.printLine()
        uint64()
    }
}
