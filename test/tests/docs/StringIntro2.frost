method process(c:Char32) {
    Console.printLine("'\{c}'")
}

method main() {
    def string := "Hello"

    var index := string.start
    while index != string.end {
        process(string[index])
        index := string.next(index)
    }
}
