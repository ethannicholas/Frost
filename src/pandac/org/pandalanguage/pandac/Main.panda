package org.pandalanguage.pandac

uses org.pandalanguage.pandac.parser.Lexer
uses org.pandalanguage.pandac.parser.Parser
uses org.pandalanguage.pandac.parser.TokenKind

class Main {
    @class
    method main() {
        def parser := Parser(Compiler())
        parser.start("Int16.panda", File("../src/panda/core/Int16.panda").readFully())
        Console.printLine(parser.file())
    }
}