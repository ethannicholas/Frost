package org.pandalanguage.pandac

-- FIXME use tuple
class Pair<A, B> {
    def first:A
    def second:B

    init(first:A, second:B) {
        self.first := first
        self.second := second
    }

    @override
    function convert():String {
        return "(\{first}, \{second})"
    }
}