package panda.collections

interface List<T> : ListView<T>, ListWriter<T>, Collection<T> {
    method filterInPlace(f:(T)=>(Bit)) {
        for i in get_count() - 1 ... 0 by -1 {
            if !f(self[i]) {
                removeIndex(i)
            }
        }
    }
}
