package panda.collections

====================================================================================================
A write-only version of the standard `Map` abstract data type. `MapWriter` is most often paired with
`MapView`, as in the [Map] interface.
====================================================================================================
interface MapWriter<K, V> {
    method []:=(key:K, value:V)
}