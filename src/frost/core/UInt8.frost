------------------------------------------------------------------
-- This file was autogenerated by 'writeNumbers', do not modify --
------------------------------------------------------------------

package frost.core

uses frost.unsafe.Pointer

===
An 8-bit unsigned integer.
===
@final
class UInt8 : Value, Key<UInt8>, Comparable<UInt8>, Formattable, ListView<Bit> {
    ===
    The smallest value this type can hold (0).
    ===
    constant MIN:UInt8 := 0

    ===
    The largest value this type can hold (255).
    ===
    constant MAX:UInt8 := 255

    @private
    def value:builtin_uint8

    @private
    @implicit
    init(value:builtin_uint8) {
        self.value := value
    }

    ===Adds another number to this number.===
    @priority(1)
    function +(other:Int8):Int32 {
        return value->builtin_int32 + other.value->builtin_int32
    }

    ===Adds another number to this number.===
    @priority(3)
    function +(other:Int16):Int32 {
        return value->builtin_int32 + other.value->builtin_int32
    }

    ===Adds another number to this number.===
    @priority(5)
    function +(other:Int32):Int64 {
        return value->builtin_int64 + other.value->builtin_int64
    }

    ===Adds another number to this number.===
    @priority(20)
    function +(other:UInt8):UInt32 {
        return value->builtin_uint32 + other.value->builtin_uint32
    }
    ===Adds another number to this number.===
    @priority(-1)
    function +(other:UInt8):UInt8 {
        return value + other.value
    }

    ===Adds another number to this number.===
    @priority(2)
    function +(other:UInt16):UInt32 {
        return value->builtin_uint32 + other.value->builtin_uint32
    }

    ===Adds another number to this number.===
    @priority(4)
    function +(other:UInt32):UInt32 {
        return value->builtin_uint32 + other.value
    }

    ===Adds another number to this number.===
    @priority(6)
    function +(other:UInt64):UInt64 {
        return value->builtin_uint64 + other.value
    }

    ===Subtracts another number from this number.===
    @priority(1)
    function -(other:Int8):Int32 {
        return value->builtin_int32 - other.value->builtin_int32
    }

    ===Subtracts another number from this number.===
    @priority(3)
    function -(other:Int16):Int32 {
        return value->builtin_int32 - other.value->builtin_int32
    }

    ===Subtracts another number from this number.===
    @priority(5)
    function -(other:Int32):Int64 {
        return value->builtin_int64 - other.value->builtin_int64
    }

    ===Subtracts another number from this number.===
    @priority(20)
    function -(other:UInt8):UInt32 {
        return value->builtin_uint32 - other.value->builtin_uint32
    }
    ===Subtracts another number from this number.===
    @priority(-1)
    function -(other:UInt8):UInt8 {
        return value - other.value
    }

    ===Subtracts another number from this number.===
    @priority(2)
    function -(other:UInt16):UInt32 {
        return value->builtin_uint32 - other.value->builtin_uint32
    }

    ===Subtracts another number from this number.===
    @priority(4)
    function -(other:UInt32):UInt32 {
        return value->builtin_uint32 - other.value
    }

    ===Subtracts another number from this number.===
    @priority(6)
    function -(other:UInt64):UInt64 {
        return value->builtin_uint64 - other.value
    }

    ===Returns the negation (additive inverse) of this number===
    function -():UInt8 {
        return UInt8(-value)
    }

    ===Multiplies this number by another number.===
    @priority(1)
    function *(other:Int8):Int32 {
        return value->builtin_int32 * other.value->builtin_int32
    }

    ===Multiplies this number by another number.===
    @priority(3)
    function *(other:Int16):Int32 {
        return value->builtin_int32 * other.value->builtin_int32
    }

    ===Multiplies this number by another number.===
    @priority(5)
    function *(other:Int32):Int64 {
        return value->builtin_int64 * other.value->builtin_int64
    }

    ===Multiplies this number by another number.===
    @priority(20)
    function *(other:UInt8):UInt32 {
        return value->builtin_uint32 * other.value->builtin_uint32
    }
    ===Multiplies this number by another number.===
    @priority(-1)
    function *(other:UInt8):UInt8 {
        return value * other.value
    }

    ===Multiplies this number by another number.===
    @priority(2)
    function *(other:UInt16):UInt32 {
        return value->builtin_uint32 * other.value->builtin_uint32
    }

    ===Multiplies this number by another number.===
    @priority(4)
    function *(other:UInt32):UInt32 {
        return value->builtin_uint32 * other.value
    }

    ===Multiplies this number by another number.===
    @priority(6)
    function *(other:UInt64):UInt64 {
        return value->builtin_uint64 * other.value
    }

    ===Divides this number by another number, returning the whole number portion.===
    @priority(1)
    function //(other:Int8):Int32 {
        return value->builtin_int32 // other.value->builtin_int32
    }

    ===Divides this number by another number, returning the whole number portion.===
    @priority(3)
    function //(other:Int16):Int32 {
        return value->builtin_int32 // other.value->builtin_int32
    }

    ===Divides this number by another number, returning the whole number portion.===
    @priority(5)
    function //(other:Int32):Int64 {
        return value->builtin_int64 // other.value->builtin_int64
    }

    ===Divides this number by another number, returning the whole number portion.===
    @priority(20)
    function //(other:UInt8):UInt32 {
        return value->builtin_uint32 // other.value->builtin_uint32
    }
    ===Divides this number by another number, returning the whole number portion.===
    @priority(-1)
    function //(other:UInt8):UInt8 {
        return value // other.value
    }

    ===Divides this number by another number, returning the whole number portion.===
    @priority(2)
    function //(other:UInt16):UInt32 {
        return value->builtin_uint32 // other.value->builtin_uint32
    }

    ===Divides this number by another number, returning the whole number portion.===
    @priority(4)
    function //(other:UInt32):UInt32 {
        return value->builtin_uint32 // other.value
    }

    ===Divides this number by another number, returning the whole number portion.===
    @priority(6)
    function //(other:UInt64):UInt64 {
        return value->builtin_uint64 // other.value
    }

    ===Returns the remainder of dividing this number by another number.===
    @priority(1)
    function %(other:Int8):Int32 {
        return value->builtin_int32 % other.value->builtin_int32
    }

    ===Returns the remainder of dividing this number by another number.===
    @priority(3)
    function %(other:Int16):Int32 {
        return value->builtin_int32 % other.value->builtin_int32
    }

    ===Returns the remainder of dividing this number by another number.===
    @priority(5)
    function %(other:Int32):Int64 {
        return value->builtin_int64 % other.value->builtin_int64
    }

    ===Returns the remainder of dividing this number by another number.===
    @priority(20)
    function %(other:UInt8):UInt32 {
        return value->builtin_uint32 % other.value->builtin_uint32
    }
    ===Returns the remainder of dividing this number by another number.===
    @priority(-1)
    function %(other:UInt8):UInt8 {
        return value % other.value
    }

    ===Returns the remainder of dividing this number by another number.===
    @priority(2)
    function %(other:UInt16):UInt32 {
        return value->builtin_uint32 % other.value->builtin_uint32
    }

    ===Returns the remainder of dividing this number by another number.===
    @priority(4)
    function %(other:UInt32):UInt32 {
        return value->builtin_uint32 % other.value
    }

    ===Returns the remainder of dividing this number by another number.===
    @priority(6)
    function %(other:UInt64):UInt64 {
        return value->builtin_uint64 % other.value
    }

    ===Divides this number by another number.===
    @priority(1)
    function /(other:Int8):Real32 {
        return value->builtin_float32 / other.value->builtin_float32
    }

    ===Divides this number by another number.===
    @priority(3)
    function /(other:Int16):Real32 {
        return value->builtin_float32 / other.value->builtin_float32
    }

    ===Divides this number by another number.===
    @priority(5)
    function /(other:Int32):Real32 {
        return value->builtin_float32 / other.value->builtin_float32
    }

    ===Divides this number by another number.===
    @priority(7)
    function /(other:Int64):Real64 {
        return value->builtin_float64 / other.value->builtin_float64
    }

    ===Divides this number by another number.===
    @priority(20)
    function /(other:UInt8):Real32 {
        return value->builtin_float32 / other.value->builtin_float32
    }

    ===Divides this number by another number.===
    @priority(2)
    function /(other:UInt16):Real32 {
        return value->builtin_float32 / other.value->builtin_float32
    }

    ===Divides this number by another number.===
    @priority(4)
    function /(other:UInt32):Real32 {
        return value->builtin_float32 / other.value->builtin_float32
    }

    ===Divides this number by another number.===
    @priority(6)
    function /(other:UInt64):Real64 {
        return value->builtin_float64 / other.value->builtin_float64
    }

    ===Divides this number by another number.===
    @priority(8)
    function /(other:Real32):Real32 {
        return value->builtin_float32 / other.value
    }

    ===Divides this number by another number.===
    @priority(9)
    function /(other:Real64):Real64 {
        return value->builtin_float64 / other.value
    }

    ===Returns the bitwise NOT of this number.===
    function !!():UInt8 {
        return UInt8(!!value)
    }

    ===Returns the bitwise AND of this number with another number.===
    @priority(1)
    function &&(other:Int8):Int32 {
        return value->builtin_int32 && other.value->builtin_int32
    }

    ===Returns the bitwise AND of this number with another number.===
    @priority(3)
    function &&(other:Int16):Int32 {
        return value->builtin_int32 && other.value->builtin_int32
    }

    ===Returns the bitwise AND of this number with another number.===
    @priority(5)
    function &&(other:Int32):Int32 {
        return value->builtin_int32 && other.value
    }

    ===Returns the bitwise AND of this number with another number.===
    @priority(7)
    function &&(other:Int64):Int64 {
        return value->builtin_int64 && other.value
    }

    ===Returns the bitwise AND of this number with another number.===
    @priority(20)
    function &&(other:UInt8):UInt32 {
        return value->builtin_uint32 && other.value->builtin_uint32
    }
    ===Returns the bitwise AND of this number with another number.===
    @priority(-1)
    function &&(other:UInt8):UInt8 {
        return value && other.value
    }

    ===Returns the bitwise AND of this number with another number.===
    @priority(2)
    function &&(other:UInt16):UInt32 {
        return value->builtin_uint32 && other.value->builtin_uint32
    }

    ===Returns the bitwise AND of this number with another number.===
    @priority(4)
    function &&(other:UInt32):UInt32 {
        return value->builtin_uint32 && other.value
    }

    ===Returns the bitwise AND of this number with another number.===
    @priority(6)
    function &&(other:UInt64):UInt64 {
        return value->builtin_uint64 && other.value
    }

    ===Returns the bitwise OR of this number with another number.===
    @priority(1)
    function ||(other:Int8):Int32 {
        return value->builtin_int32 || other.value->builtin_int32
    }

    ===Returns the bitwise OR of this number with another number.===
    @priority(3)
    function ||(other:Int16):Int32 {
        return value->builtin_int32 || other.value->builtin_int32
    }

    ===Returns the bitwise OR of this number with another number.===
    @priority(5)
    function ||(other:Int32):Int32 {
        return value->builtin_int32 || other.value
    }

    ===Returns the bitwise OR of this number with another number.===
    @priority(7)
    function ||(other:Int64):Int64 {
        return value->builtin_int64 || other.value
    }

    ===Returns the bitwise OR of this number with another number.===
    @priority(20)
    function ||(other:UInt8):UInt32 {
        return value->builtin_uint32 || other.value->builtin_uint32
    }
    ===Returns the bitwise OR of this number with another number.===
    @priority(-1)
    function ||(other:UInt8):UInt8 {
        return value || other.value
    }

    ===Returns the bitwise OR of this number with another number.===
    @priority(2)
    function ||(other:UInt16):UInt32 {
        return value->builtin_uint32 || other.value->builtin_uint32
    }

    ===Returns the bitwise OR of this number with another number.===
    @priority(4)
    function ||(other:UInt32):UInt32 {
        return value->builtin_uint32 || other.value
    }

    ===Returns the bitwise OR of this number with another number.===
    @priority(6)
    function ||(other:UInt64):UInt64 {
        return value->builtin_uint64 || other.value
    }

    ===Returns the bitwise XOR of this number with another number.===
    @priority(1)
    function ~~(other:Int8):Int32 {
        return value->builtin_int32 ~~ other.value->builtin_int32
    }

    ===Returns the bitwise XOR of this number with another number.===
    @priority(3)
    function ~~(other:Int16):Int32 {
        return value->builtin_int32 ~~ other.value->builtin_int32
    }

    ===Returns the bitwise XOR of this number with another number.===
    @priority(5)
    function ~~(other:Int32):Int32 {
        return value->builtin_int32 ~~ other.value
    }

    ===Returns the bitwise XOR of this number with another number.===
    @priority(7)
    function ~~(other:Int64):Int64 {
        return value->builtin_int64 ~~ other.value
    }

    ===Returns the bitwise XOR of this number with another number.===
    @priority(20)
    function ~~(other:UInt8):UInt32 {
        return value->builtin_uint32 ~~ other.value->builtin_uint32
    }
    ===Returns the bitwise XOR of this number with another number.===
    @priority(-1)
    function ~~(other:UInt8):UInt8 {
        return value ~~ other.value
    }

    ===Returns the bitwise XOR of this number with another number.===
    @priority(2)
    function ~~(other:UInt16):UInt32 {
        return value->builtin_uint32 ~~ other.value->builtin_uint32
    }

    ===Returns the bitwise XOR of this number with another number.===
    @priority(4)
    function ~~(other:UInt32):UInt32 {
        return value->builtin_uint32 ~~ other.value
    }

    ===Returns the bitwise XOR of this number with another number.===
    @priority(6)
    function ~~(other:UInt64):UInt64 {
        return value->builtin_uint64 ~~ other.value
    }

    ===Returns this number shifted left by the specified number of bits.===
    @priority(1)
    function <<(other:Int8):Int32 {
        return value->builtin_int32 << other.value->builtin_int32
    }

    ===Returns this number shifted left by the specified number of bits.===
    @priority(3)
    function <<(other:Int16):Int32 {
        return value->builtin_int32 << other.value->builtin_int32
    }

    ===Returns this number shifted left by the specified number of bits.===
    @priority(5)
    function <<(other:Int32):Int32 {
        return value->builtin_int32 << other.value
    }

    ===Returns this number shifted left by the specified number of bits.===
    @priority(7)
    function <<(other:Int64):Int64 {
        return value->builtin_int64 << other.value
    }

    ===Returns this number shifted left by the specified number of bits.===
    @priority(20)
    function <<(other:UInt8):UInt32 {
        return value->builtin_uint32 << other.value->builtin_uint32
    }
    ===Returns this number shifted left by the specified number of bits.===
    @priority(-1)
    function <<(other:UInt8):UInt8 {
        return value << other.value
    }

    ===Returns this number shifted left by the specified number of bits.===
    @priority(2)
    function <<(other:UInt16):UInt32 {
        return value->builtin_uint32 << other.value->builtin_uint32
    }

    ===Returns this number shifted left by the specified number of bits.===
    @priority(4)
    function <<(other:UInt32):UInt32 {
        return value->builtin_uint32 << other.value
    }

    ===Returns this number shifted left by the specified number of bits.===
    @priority(6)
    function <<(other:UInt64):UInt64 {
        return value->builtin_uint64 << other.value
    }

    ===Returns this number logical shifted right by the specified number of bits.===
    @priority(1)
    function >>(other:Int8):Int32 {
        return value->builtin_int32 >> other.value->builtin_int32
    }

    ===Returns this number logical shifted right by the specified number of bits.===
    @priority(3)
    function >>(other:Int16):Int32 {
        return value->builtin_int32 >> other.value->builtin_int32
    }

    ===Returns this number logical shifted right by the specified number of bits.===
    @priority(5)
    function >>(other:Int32):Int32 {
        return value->builtin_int32 >> other.value
    }

    ===Returns this number logical shifted right by the specified number of bits.===
    @priority(7)
    function >>(other:Int64):Int64 {
        return value->builtin_int64 >> other.value
    }

    ===Returns this number logical shifted right by the specified number of bits.===
    @priority(20)
    function >>(other:UInt8):UInt32 {
        return value->builtin_uint32 >> other.value->builtin_uint32
    }
    ===Returns this number logical shifted right by the specified number of bits.===
    @priority(-1)
    function >>(other:UInt8):UInt8 {
        return value >> other.value
    }

    ===Returns this number logical shifted right by the specified number of bits.===
    @priority(2)
    function >>(other:UInt16):UInt32 {
        return value->builtin_uint32 >> other.value->builtin_uint32
    }

    ===Returns this number logical shifted right by the specified number of bits.===
    @priority(4)
    function >>(other:UInt32):UInt32 {
        return value->builtin_uint32 >> other.value
    }

    ===Returns this number logical shifted right by the specified number of bits.===
    @priority(6)
    function >>(other:UInt64):UInt64 {
        return value->builtin_uint64 >> other.value
    }

    ===Returns `true` if this number is equal to the given number.===
    @priority(1)
    function =(other:Int8):Bit {
        return value->builtin_int16 = other.value->builtin_int16
    }

    ===Returns `true` if this number is equal to the given number.===
    @priority(3)
    function =(other:Int16):Bit {
        return value->builtin_int32 = other.value->builtin_int32
    }

    ===Returns `true` if this number is equal to the given number.===
    @priority(5)
    function =(other:Int32):Bit {
        return value->builtin_int64 = other.value->builtin_int64
    }

    ===Returns `true` if this number is equal to the given number.===
    @override
    @priority(20)
    function =(other:UInt8):Bit {
        return value = other.value
    }

    ===Returns `true` if this number is equal to the given number.===
    @priority(2)
    function =(other:UInt16):Bit {
        return value->builtin_uint16 = other.value
    }

    ===Returns `true` if this number is equal to the given number.===
    @priority(4)
    function =(other:UInt32):Bit {
        return value->builtin_uint32 = other.value
    }

    ===Returns `true` if this number is equal to the given number.===
    @priority(6)
    function =(other:UInt64):Bit {
        return value->builtin_uint64 = other.value
    }

    ===Returns `true` if this number is not equal to the given number.===
    @priority(1)
    function !=(other:Int8):Bit {
        return value->builtin_int16 != other.value->builtin_int16
    }

    ===Returns `true` if this number is not equal to the given number.===
    @priority(3)
    function !=(other:Int16):Bit {
        return value->builtin_int32 != other.value->builtin_int32
    }

    ===Returns `true` if this number is not equal to the given number.===
    @priority(5)
    function !=(other:Int32):Bit {
        return value->builtin_int64 != other.value->builtin_int64
    }

    ===Returns `true` if this number is not equal to the given number.===
    @override
    @priority(20)
    function !=(other:UInt8):Bit {
        return value != other.value
    }

    ===Returns `true` if this number is not equal to the given number.===
    @priority(2)
    function !=(other:UInt16):Bit {
        return value->builtin_uint16 != other.value
    }

    ===Returns `true` if this number is not equal to the given number.===
    @priority(4)
    function !=(other:UInt32):Bit {
        return value->builtin_uint32 != other.value
    }

    ===Returns `true` if this number is not equal to the given number.===
    @priority(6)
    function !=(other:UInt64):Bit {
        return value->builtin_uint64 != other.value
    }

    ===Returns `true` if this number is less than the given number.===
    @priority(1)
    function <(other:Int8):Bit {
        return value->builtin_int16 < other.value->builtin_int16
    }

    ===Returns `true` if this number is less than the given number.===
    @priority(3)
    function <(other:Int16):Bit {
        return value->builtin_int32 < other.value->builtin_int32
    }

    ===Returns `true` if this number is less than the given number.===
    @priority(5)
    function <(other:Int32):Bit {
        return value->builtin_int64 < other.value->builtin_int64
    }

    ===Returns `true` if this number is less than the given number.===
    @override
    @priority(20)
    function <(other:UInt8):Bit {
        return value < other.value
    }

    ===Returns `true` if this number is less than the given number.===
    @priority(2)
    function <(other:UInt16):Bit {
        return value->builtin_uint16 < other.value
    }

    ===Returns `true` if this number is less than the given number.===
    @priority(4)
    function <(other:UInt32):Bit {
        return value->builtin_uint32 < other.value
    }

    ===Returns `true` if this number is less than the given number.===
    @priority(6)
    function <(other:UInt64):Bit {
        return value->builtin_uint64 < other.value
    }

    ===Returns `true` if this number is greater than the given number.===
    @priority(1)
    function >(other:Int8):Bit {
        return value->builtin_int16 > other.value->builtin_int16
    }

    ===Returns `true` if this number is greater than the given number.===
    @priority(3)
    function >(other:Int16):Bit {
        return value->builtin_int32 > other.value->builtin_int32
    }

    ===Returns `true` if this number is greater than the given number.===
    @priority(5)
    function >(other:Int32):Bit {
        return value->builtin_int64 > other.value->builtin_int64
    }

    ===Returns `true` if this number is greater than the given number.===
    @override
    @priority(20)
    function >(other:UInt8):Bit {
        return value > other.value
    }

    ===Returns `true` if this number is greater than the given number.===
    @priority(2)
    function >(other:UInt16):Bit {
        return value->builtin_uint16 > other.value
    }

    ===Returns `true` if this number is greater than the given number.===
    @priority(4)
    function >(other:UInt32):Bit {
        return value->builtin_uint32 > other.value
    }

    ===Returns `true` if this number is greater than the given number.===
    @priority(6)
    function >(other:UInt64):Bit {
        return value->builtin_uint64 > other.value
    }

    ===Returns `true` if this number is greater than or equal to the given number.===
    @priority(1)
    function >=(other:Int8):Bit {
        return value->builtin_int16 >= other.value->builtin_int16
    }

    ===Returns `true` if this number is greater than or equal to the given number.===
    @priority(3)
    function >=(other:Int16):Bit {
        return value->builtin_int32 >= other.value->builtin_int32
    }

    ===Returns `true` if this number is greater than or equal to the given number.===
    @priority(5)
    function >=(other:Int32):Bit {
        return value->builtin_int64 >= other.value->builtin_int64
    }

    ===Returns `true` if this number is greater than or equal to the given number.===
    @override
    @priority(20)
    function >=(other:UInt8):Bit {
        return value >= other.value
    }

    ===Returns `true` if this number is greater than or equal to the given number.===
    @priority(2)
    function >=(other:UInt16):Bit {
        return value->builtin_uint16 >= other.value
    }

    ===Returns `true` if this number is greater than or equal to the given number.===
    @priority(4)
    function >=(other:UInt32):Bit {
        return value->builtin_uint32 >= other.value
    }

    ===Returns `true` if this number is greater than or equal to the given number.===
    @priority(6)
    function >=(other:UInt64):Bit {
        return value->builtin_uint64 >= other.value
    }

    ===Returns `true` if this number is less than or equal to the given number.===
    @priority(1)
    function <=(other:Int8):Bit {
        return value->builtin_int16 <= other.value->builtin_int16
    }

    ===Returns `true` if this number is less than or equal to the given number.===
    @priority(3)
    function <=(other:Int16):Bit {
        return value->builtin_int32 <= other.value->builtin_int32
    }

    ===Returns `true` if this number is less than or equal to the given number.===
    @priority(5)
    function <=(other:Int32):Bit {
        return value->builtin_int64 <= other.value->builtin_int64
    }

    ===Returns `true` if this number is less than or equal to the given number.===
    @override
    @priority(20)
    function <=(other:UInt8):Bit {
        return value <= other.value
    }

    ===Returns `true` if this number is less than or equal to the given number.===
    @priority(2)
    function <=(other:UInt16):Bit {
        return value->builtin_uint16 <= other.value
    }

    ===Returns `true` if this number is less than or equal to the given number.===
    @priority(4)
    function <=(other:UInt32):Bit {
        return value->builtin_uint32 <= other.value
    }

    ===Returns `true` if this number is less than or equal to the given number.===
    @priority(6)
    function <=(other:UInt64):Bit {
        return value->builtin_uint64 <= other.value
    }

    ===Returns the smaller (closest to negative infinity) of this number and another number.===
    @priority(1)
    function min(other:Int8):Int16 {
        if value->builtin_int16 < other.value->builtin_int16 {
            return value->builtin_int16
        }
        return other.value->builtin_int16
    }

    ===Returns the smaller (closest to negative infinity) of this number and another number.===
    @priority(3)
    function min(other:Int16):Int32 {
        if value->builtin_int32 < other.value->builtin_int32 {
            return value->builtin_int32
        }
        return other.value->builtin_int32
    }

    ===Returns the smaller (closest to negative infinity) of this number and another number.===
    @priority(5)
    function min(other:Int32):Int64 {
        if value->builtin_int64 < other.value->builtin_int64 {
            return value->builtin_int64
        }
        return other.value->builtin_int64
    }


    ===Returns the smaller (closest to negative infinity) of this number and another number.===
    @priority(20)
    function min(other:UInt8):UInt8 {
        if value < other.value {
            return value
        }
        return other.value
    }

    ===Returns the smaller (closest to negative infinity) of this number and another number.===
    @priority(2)
    function min(other:UInt16):UInt16 {
        if value->builtin_uint16 < other.value {
            return value->builtin_uint16
        }
        return other.value
    }

    ===Returns the smaller (closest to negative infinity) of this number and another number.===
    @priority(4)
    function min(other:UInt32):UInt32 {
        if value->builtin_uint32 < other.value {
            return value->builtin_uint32
        }
        return other.value
    }

    ===Returns the smaller (closest to negative infinity) of this number and another number.===
    @priority(6)
    function min(other:UInt64):UInt64 {
        if value->builtin_uint64 < other.value {
            return value->builtin_uint64
        }
        return other.value
    }

    ===Returns the larger (closest to positive infinity) of this number and another number.===
    @priority(1)
    function max(other:Int8):Int16 {
        if value->builtin_int16 > other.value->builtin_int16 {
            return value->builtin_int16
        }
        return other.value->builtin_int16
    }

    ===Returns the larger (closest to positive infinity) of this number and another number.===
    @priority(3)
    function max(other:Int16):Int32 {
        if value->builtin_int32 > other.value->builtin_int32 {
            return value->builtin_int32
        }
        return other.value->builtin_int32
    }

    ===Returns the larger (closest to positive infinity) of this number and another number.===
    @priority(5)
    function max(other:Int32):Int64 {
        if value->builtin_int64 > other.value->builtin_int64 {
            return value->builtin_int64
        }
        return other.value->builtin_int64
    }


    ===Returns the larger (closest to positive infinity) of this number and another number.===
    @priority(20)
    function max(other:UInt8):UInt8 {
        if value > other.value {
            return value
        }
        return other.value
    }

    ===Returns the larger (closest to positive infinity) of this number and another number.===
    @priority(2)
    function max(other:UInt16):UInt16 {
        if value->builtin_uint16 > other.value {
            return value->builtin_uint16
        }
        return other.value
    }

    ===Returns the larger (closest to positive infinity) of this number and another number.===
    @priority(4)
    function max(other:UInt32):UInt32 {
        if value->builtin_uint32 > other.value {
            return value->builtin_uint32
        }
        return other.value
    }

    ===Returns the larger (closest to positive infinity) of this number and another number.===
    @priority(6)
    function max(other:UInt64):UInt64 {
        if value->builtin_uint64 > other.value {
            return value->builtin_uint64
        }
        return other.value
    }
    ===Returns a list of all integers in the given range. The list is 'lazy', meaning that it does not actually allocate memory to hold the entire list.===
    @class
    function [](range:Range<UInt8>):ListView<UInt8> {
        return org.frostlanguage.frost.UInt8List(SteppedRange<UInt8, UInt8>(range.min, range.max, 1, range.inclusive))
    }
    ===Returns a list of all integers in the given stepped range. The list is 'lazy', meaning that it does not actually allocate memory to hold the entire list.===
    @class
    function [](range:SteppedRange<UInt8, UInt8>):ListView<UInt8> {
        return org.frostlanguage.frost.UInt8List(range)
    }

    ===Returns a single bit from this number, where index 0 is the least significant bit.===
    @override
    function [](index:Int64):Bit {
        return self && (1 << index) != 0
    }

    ===Returns the number of bits in this integer, which is always 8.===
    @override
    function get_count():Int {
        return 8
    }

    @override
    function get_iterator():Iterator<Bit> {
        return org.frostlanguage.frost.IntBitIterator(self.convert()->UInt64, 1 << 7)
    }

    @override
    function get_hash():Int {
        return Int(value->builtin_int64)
    }

    ===Parses a string as a number in the specified radix. Returns `null` if the parse fails.===
    @class
    @pre(radix >= 2 & radix <= 36)
    function parse(str:String, radix:Int):UInt8? {
        def result := Frost.parse(str, radix)
        if result == null {
            return null
        }
        return result.convert()->UInt8
    }

    ===Returns this number reinterpreted to an bit signed number.===
    function convert():Int8 {
        return Int8(value->builtin_int8)
    }

    ===Returns this number zero extended to a bit signed number.===
    function convert():Int16 {
        return Int16(value->builtin_int16)
    }

    ===Returns this number zero extended to a bit signed number.===
    function convert():Int32 {
        return Int32(value->builtin_int32)
    }

    ===Returns this number zero extended to a bit signed number.===
    function convert():Int64 {
        return Int64(value->builtin_int64)
    }

    ===Returns this number zero extended to a bit unsigned number.===
    function convert():UInt16 {
        return UInt16(value->builtin_uint16)
    }

    ===Returns this number zero extended to a bit unsigned number.===
    function convert():UInt32 {
        return UInt32(value->builtin_uint32)
    }

    ===Returns this number zero extended to a bit unsigned number.===
    function convert():UInt64 {
        return UInt64(value->builtin_uint64)
    }

    ===Returns this number zero extended to a bit signed number.===
    function convert():Real32 {
        return Real32(value->builtin_float32)
    }

    ===Returns this number zero extended to a bit signed number.===
    function convert():Real64 {
        return Real64(value->builtin_float64)
    }

    ===Returns this number converted to a decimal string.===
    @override
    function convert():String {
        constant max := 3
        def chars := Pointer<Char8>.alloc(max)
        var index := max - 1
        var value := self
        if value >= 0 {
            do {
                chars[index] := Char8((value % 10 + 48).convert())
                value := (value // 10).convert()->UInt8
                index -= 1
            }
            while value > 0
            index += 1
        }
        else {
            do {
                chars[index] := Char8((48 - value % 10).convert())
                value := (value // 10).convert()->UInt8
                index -= 1
            }
            while value < 0
            chars[index] := "-"
        }
        def size := max - index
        for i in 0 .. size {
            chars[i] := chars[i + index]
        }
        return String(chars, size)
    }

    === Returns a formatted representation of this number. Supported format strings are `""`, `"d"`, or `"D"` for decimal, `"b"` or `"B"` for binary, `"o"` or `"O"` for octal, `"x"` for lowercase hexadecimal, and `"X"` for uppercase hexadecimal. ===
    @override
    function format(fmt:String):String {
        return Frost.format(false, self.convert()->UInt64, 0xFF, fmt)
    }
}
