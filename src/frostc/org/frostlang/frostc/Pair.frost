package org.frostlang.frostc

-- FIXME use tuple
class Pair<A, B> {
    def first:A
    def second:B

    init(first:A, second:B) {
        self.first := first
        self.second := second
    }

    @override
    function get_asString():String {
        def firstString:String
        if first !== null {
            firstString := first.asString
        }
        else {
            firstString := "<null>"
        }
        def secondString:String
        if second !== null {
            secondString := second.asString
        }
        else {
            secondString := "<null>"
        }
        return "(\{firstString}, \{secondString})"
    }
}